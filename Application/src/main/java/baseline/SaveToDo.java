package baseline;

public class SaveToDo {

    //Takes selected link list Items
    //Saves them to a file in a specific format

    //for (Number of items in Linked List)
        // IF (item.selected == true)
        //      Save To-Do Title
        //      Save items in To-Do and whether they are complete
}
